# Linmas 0.1.1

## Summary
First automated patch release using the new `main` + `vX.Y.Z` release flow.

## Included changes
- release CI/CD workflows are active
- release provenance workflow is active
- CI hardening now uses Node 24 and a committed lockfile
- host-detection test fixture is runner-portable

## Operator note
Future releases should continue through `main` and `vX.Y.Z` tags.
