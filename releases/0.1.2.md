# Linmas 0.1.2

## Summary
Clean automated patch release using the existing `main` + `vX.Y.Z` workflow.

## Included changes
- release automation is active through `main` + `vX.Y.Z`
- provenance workflow remains active
- CI hardening uses Node 24 and a committed lockfile
- stronger rehearsal checks now support the release operator flow

## Operator note
Future releases should continue through `main` and `vX.Y.Z` tags.
